services:
  telegram-audio-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: telegram-audio-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./storage:/app/storage
